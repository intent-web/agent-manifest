# WordPress AgentManifest Examples

WordPress adoption should work like SEO plugin adoption: site owners configure
identity, knowledge, policies and capabilities in the admin UI, then the plugin
publishes static AI-readable files and optional action gateways.

The `core/` manifest covers normal WordPress sites. Commerce plugin examples
extend that model with product catalogs, carts, checkout preparation, order risk
and audit requirements.

