# Web Framework AgentManifest Examples

Framework manifests focus on deployment shape: static generation, SPA fallback,
SSR, API routes, serverless functions or full backend runtimes.

The most important implementation rule for frontend apps is to serve
AgentManifest and AI files as real files or explicit routes. They must not be
swallowed by a single-page application fallback.

